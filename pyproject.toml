[project]
name = "pangur"
version = "0.1.0"
description = "Copy and mirror files"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "black>=25.11.0",
    "pytest>=8.4.2",
    "ruff>=0.15.12",
    "ty>=0.0.34",
]
